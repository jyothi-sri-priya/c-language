#include <stdio.h>

int main()
{
    int x;
    printf("enter number:");
    scanf("%d",&x);
    int ans=checkevenodd(x);
    if(ans==0)
    printf("even");
    else
    printf("odd");
}
void checkevenodd(int x)
{
    if(x%2==0)
    return(0);
    else
    return(1);
}
