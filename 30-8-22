#include <stdio.h>
int main()
{
    int i;
    char s[1000];
    printf("enter string:");
    gets(s);
    for(i=0;s[i]!='\0';i++)
    {
        if((s[i]=='a'))
        {
            s[i]=s[i]-32;;
        }
    }
    puts(s);
    return 0;
}
